# Configuration Files

Store reviewed YAML or JSON configurations here.

Each configuration should state:

- dataset release;
- split identifier;
- sequence length and stride;
- feature list;
- model architecture;
- optimisation settings;
- seed;
- threshold-selection rule;
- output directory expressed as a relative path or environment variable.
