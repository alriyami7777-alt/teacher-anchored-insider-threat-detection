# Source Code

Only reviewed, portable implementation code should be copied here.

Code copied from the full experimental repository must be checked for:

- absolute paths;
- dataset identifiers;
- hidden dependencies on local worktrees;
- checkpoint loading;
- hard-coded thresholds;
- test-set access;
- untracked configuration assumptions.
