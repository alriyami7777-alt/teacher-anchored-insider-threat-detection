# Tests

Minimum tests should cover:

- sequence shape and feature count;
- chronological split boundaries;
- prevention of cross-boundary windows;
- threshold selection from validation data only;
- student inference without teacher dependency;
- deterministic seed handling where supported.
